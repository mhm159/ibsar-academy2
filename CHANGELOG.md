# سجل التعديلات (CHANGELOG)

## التحديثات الأخيرة (أغسطس 2026)

### 🚀 الميزات الجديدة (New Features)
- **المرحلة 2 (لوحات التحكم - Dashboards):**
  - إضافة مسارات وواجهات لوحة تحكم الإدارة (Admin Dashboard): المراجعات، الحوالات المالية، المستخدمين، تسعير المعلمين والعمولات (`admin/teacher-pricing`, `admin/commission`).
  - إضافة مسارات وواجهات لوحة تحكم ولي الأمر (Parent Dashboard): التنبيهات، التقارير، الطلاب، الجلسات، التلعيب (Gamification)، ولوحة المتصدرين.
  - إضافة مسارات وواجهات لوحة تحكم المعلم (Teacher Dashboard): الجدول، الطلاب المحجوزين، طلبات السحب (Payouts)، والمحافظ المالية (Wallets).

- **المرحلة 3 (المدفوعات - Payments):**
  - تفعيل مسارات الدفع وواجهة `checkout`.
  - إضافة نظام `Webhooks` للتعامل التلقائي مع عمليات الدفع من (Paymob) و (Stripe).
  - إضافة نظام حجز الأموال (Escrow) لحماية حقوق المعلمين والمنصة.

- **نظام الواجبات (Homework System):**
  - إضافة محرر واجبات تفاعلي (`interactive-homework-editor.tsx`).
  - إضافة مشغل الواجبات للطلاب (`homework-player.tsx`).
  - مسارات خاصة لعرض وتقييم الواجبات لكل من المعلم وولي الأمر.

- **الفصول الافتراضية (Classroom):**
  - تحسين ميزات السبورة البيضاء (Whiteboard) ومربع المحادثة (Chat) والفيديو في الغرف الافتراضية.

### 🛠️ تحسينات وأدوات (Improvements & Tools)
- إصلاح مشاكل التوافق عبر المتصفحات (Cross-Browser) في ملف `globals.css` (تحسين خطوط Safari وأشرطة تمرير Firefox).
- إضافة سكربتات لتسهيل الإعداد: `fix-accounts.bat`، `ensure-admin.ts` وغيرها.
- إضافة ميزة الإشعارات المنبثقة (`popup-notification.tsx`) وتكامل مع تطبيق واتساب (`whatsapp.ts`).
- إضافة Seeders لقاعدة البيانات (لإدخال بيانات اختبارية للمدفوعات ونظام التلعيب).

### 🐛 إصلاحات الأخطاء (Bug Fixes)
- حل مشكلة تباين عرض الواجهات على متصفح Chrome بسبب الكاش والإضافات (Extensions).
